@page
@model TeamUP.Pages.Party.TeamWorksPage
@{
    Layout = "_Index";
}
<body>
<table class="table">
    <thead>
        <tr>
            <th> @Html.DisplayNameFor(model => model.Items[0].Overview) </th>
            <th> @Html.DisplayNameFor(model => model.Items[0].Name) </th>
            <th> @Html.DisplayNameFor(model => model.Items[0].Description) </th>
            <th> @Html.DisplayNameFor(model => model.Items[0].TeamSize) </th>
            <th> @Html.DisplayNameFor(model => model.Items[0].Deadline) </th>
            <th> @Html.DisplayNameFor(model => model.Items[0].Done) </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Items) {
        <tr>
            <td> @Html.DisplayFor(modelItem => item.Overview) </td>
            <td> @Html.DisplayFor(modelItem => item.Name) </td>
            <td> @Html.DisplayFor(modelItem => item.Description) </td>
            <td> @Html.DisplayFor(modelItem => item.TeamSize) </td>
            <td> @Html.DisplayFor(modelItem => item.Deadline) </td>
            <td> @Html.DisplayFor(modelItem => item.Done) </td>
            <td>
                <a asp-page="./Edit" 
                   asp-page-handler = "Edit" 
                   asp-route-id="@item.Id"
                   asp-route-sortOrder="@Model.CurrentSort"
                   asp-route-pageIndex = "@Model.PageIndex"
                   asp-route-currentFilter="@Model.CurrentFilter"
                >Edit</a> |
                <a asp-page="./Details"
                   asp-page-handler = "Details" 
                   asp-route-id="@item.Id"
                   asp-route-sortOrder="@Model.CurrentSort"
                   asp-route-pageIndex = "@Model.PageIndex"
                   asp-route-currentFilter="@Model.CurrentFilter"
                >Details</a> |
                <a asp-page="./Delete" 
                   asp-page-handler = "Delete"
                   asp-route-id="@item.Id"
                   asp-route-sortOrder="@Model.CurrentSort"
                   asp-route-pageIndex = "@Model.PageIndex"
                   asp-route-currentFilter="@Model.CurrentFilter"
                >Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>